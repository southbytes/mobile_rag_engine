// Basic unit test for mobile_rag_engine package
import 'package:flutter_test/flutter_test.dart';
import 'package:mobile_rag_engine/mobile_rag_engine.dart';

void main() {
  test('package exports are accessible', () {
    // Verify that the package exports are accessible
    // Actual functionality tests require model files and Rust initialization
    expect(EmbeddingService, isNotNull);
  });
}
